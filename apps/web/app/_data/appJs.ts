export const APP_JS = "\n(function(){\n  // nav shadow on scroll\n  var nav=document.getElementById('nav');\n  window.addEventListener('scroll',function(){nav.classList.toggle('scrolled',window.scrollY>12);},{passive:true});\n\n  // scroll reveal\n  var io=new IntersectionObserver(function(es){es.forEach(function(e){if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target);}});},{threshold:.14});\n  document.querySelectorAll('.reveal').forEach(function(el){io.observe(el);});\n\n  // steps connecting line draws on scroll\n  var stepsEl=document.querySelector('.steps');\n  if(stepsEl){ var sio=new IntersectionObserver(function(es){es.forEach(function(e){if(e.isIntersecting){stepsEl.classList.add('drawn');sio.disconnect();}});},{threshold:.3}); sio.observe(stepsEl); }\n\n  // marquee chips (teal/violet only \u2014 palette tightened)\n  var rowA=[['Vercel AI SDK','#2fe6c6'],['OpenAI','#9a8bff'],['Anthropic','#2fe6c6'],['LangChain','#9a8bff'],['MCP','#2fe6c6'],['TypeScript','#9a8bff'],['Node.js','#2fe6c6'],['Slack','#9a8bff'],['Stripe','#2fe6c6'],['Postgres','#9a8bff']];\n  var rowB=[['Durable ledger','#2fe6c6'],['Exactly-once','#9a8bff'],['Idempotency keys','#2fe6c6'],['Human approvals','#9a8bff'],['Auto-rollback sagas','#2fe6c6'],['Crash-safe replay','#9a8bff'],['Real-time alerts','#2fe6c6'],['Audit trail','#9a8bff'],['Multi-region','#2fe6c6']];\n  function chip(d){return '<span class=\\'mchip\\'><span class=\\'mdot\\' style=\\'background:'+d[1]+'\\'></span>'+d[0]+'</span>';}\n  function fill(id,data){var h=data.map(chip).join('');var el=document.getElementById(id);el.innerHTML=h;el.nextElementSibling.innerHTML=h;}\n  fill('mt1',rowA);fill('mt2',rowB);\n\n  // live ledger stream\n  var actions=[\n    ['issue_refund','run'],['issue_refund','wait'],['issue_refund','approved'],\n    ['charge_card','run'],['charge_card','ok'],\n    ['send_email','dedupe'],['send_email','ok'],\n    ['sync_crm','run'],['sync_crm','ok'],\n    ['provision_seat','run'],['provision_seat','ok'],\n    ['webhook.retry','dedupe'],\n    ['export_invoice','run'],['export_invoice','ok']\n  ];\n  var labels={run:['running','run'],ok:['committed','ok'],dedupe:['deduped','dedupe'],wait:['awaiting approval','wait'],approved:['approved by blue','approved']};\n  var body=document.getElementById('ledger');var idx=0;\n  function ts(){var d=new Date();return d.toTimeString().slice(0,8);}\n  function pushLedger(name,state){ if(!body)return; var l=labels[state]||labels.ok; var row=document.createElement('div');row.className='row'; row.innerHTML='<span class=\\'t\\'>'+ts()+'</span><span class=\\'a\\'><b>'+name+'</b></span><span class=\\'pill '+l[1]+'\\'>'+l[0]+'</span>'; body.appendChild(row); while(body.children.length>7){body.removeChild(body.firstChild);} body.scrollTop=body.scrollHeight; }\n  function add(){ var a=actions[idx%actions.length];idx++; pushLedger(a[0],a[1]); }\n  for(var k=0;k<5;k++){add();}\n  setInterval(add,1700);\n\n  // npm copy (Resend-style)\n  var npmBtn=document.getElementById('npmCopy');\n  if(npmBtn){ npmBtn.addEventListener('click',function(){ var t='npm i @quorvel/core'; function done(){npmBtn.textContent='Copied \u2713';npmBtn.classList.add('copied');setTimeout(function(){npmBtn.textContent='Copy';npmBtn.classList.remove('copied');},1600);} if(navigator.clipboard&&navigator.clipboard.writeText){navigator.clipboard.writeText(t).then(done).catch(done);}else{done();} }); }\n\n  // code framework tabs (Stripe-style)\n  document.querySelectorAll('.ctab').forEach(function(tb){ tb.addEventListener('click',function(){ var name=tb.getAttribute('data-tab'); document.querySelectorAll('.ctab').forEach(function(x){x.classList.toggle('active',x===tb);}); document.querySelectorAll('.cpane').forEach(function(p){p.hidden=p.getAttribute('data-pane')!==name;}); }); });\n\n  // interactive approval card (Clerk-style: you're in control)\n  var scApp=document.getElementById('scApproval');\n  if(scApp){ var apr=scApp.querySelector('.sc-approve'),rej=scApp.querySelector('.sc-reject'),btns=scApp.querySelector('.sc-btn-row'),done=scApp.querySelector('.sc-done');\n    function scResolve(ok){ btns.style.display='none'; done.hidden=false; done.textContent=ok?'\u2713 approved \u00b7 committed to ledger':'\u2715 rejected \u00b7 rolled back'; done.className='sc-done'+(ok?'':' rej'); if(ok)pushLedger('issue_refund','approved'); setTimeout(function(){ btns.style.display='flex'; done.hidden=true; },4200); }\n    if(apr)apr.addEventListener('click',function(){scResolve(true);});\n    if(rej)rej.addEventListener('click',function(){scResolve(false);});\n  }\n\n  // ---- waitlist modal: one real conversion action ----\n  // Formspree endpoint wired below. Submissions POST here AND save to localStorage as backup.\n  var WAITLIST_ENDPOINT='https://formspree.io/f/xrewbpaw';\n  var wl=document.getElementById('waitlist');\n  var wlForm=document.getElementById('wl-form');\n  var wlEmail=document.getElementById('wl-email');\n  var wlPlan=document.getElementById('wl-plan');\n  var wlSuccess=document.getElementById('wl-success');\n  var wlReturnFocus=null;\n  function trapTab(e){ if(e.key!=='Tab'||!wl.classList.contains('open'))return; var f=Array.prototype.slice.call(wl.querySelectorAll('button, [href], input, select, textarea')).filter(function(el){return !el.hidden&&el.offsetParent!==null;}); if(!f.length)return; var first=f[0],last=f[f.length-1]; if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();} else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();} }\n  function openWL(plan){ if(!wl)return; wlReturnFocus=document.activeElement; if(wlPlan)wlPlan.value=plan||'start'; if(wlSuccess)wlSuccess.hidden=true; if(wlForm)wlForm.hidden=false; wl.classList.add('open'); wl.setAttribute('aria-hidden','false'); document.body.style.overflow='hidden'; document.addEventListener('keydown',trapTab); setTimeout(function(){if(wlEmail)wlEmail.focus();},60); }\n  function closeWL(){ if(!wl)return; wl.classList.remove('open'); wl.setAttribute('aria-hidden','true'); document.body.style.overflow=''; document.removeEventListener('keydown',trapTab); if(wlReturnFocus&&wlReturnFocus.focus){wlReturnFocus.focus();} wlReturnFocus=null; }\n  function closeMobile(){ var mm=document.getElementById('mobileMenu'),hb=document.getElementById('hbtn'); if(mm)mm.classList.remove('open'); if(hb){hb.classList.remove('open');hb.setAttribute('aria-expanded','false');} }\n  function closeMenus(except){ document.querySelectorAll('.menu-item').forEach(function(it){ if(it!==except){ it.classList.remove('open'); var t=it.querySelector('.trig'); if(t)t.setAttribute('aria-expanded','false'); } }); }\n  document.querySelectorAll('[data-plan]').forEach(function(b){\n    b.addEventListener('click',function(e){ e.preventDefault(); closeMobile(); openWL(b.getAttribute('data-plan')); });\n  });\n  if(wl){ wl.addEventListener('click',function(e){ if(e.target===wl)closeWL(); }); var xb=wl.querySelector('.wl-close'); if(xb)xb.addEventListener('click',closeWL); }\n  if(wlForm){ wlForm.addEventListener('submit',function(e){ e.preventDefault(); var email=(wlEmail.value||'').trim(), plan=(wlPlan.value||'start'); if(!email)return; if(WAITLIST_ENDPOINT){ try{ fetch(WAITLIST_ENDPOINT,{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({email:email,plan:plan})}); }catch(err){} } try{ var key='qv_waitlist',arr=JSON.parse(localStorage.getItem(key)||'[]'); arr.push({email:email,plan:plan,t:Date.now()}); localStorage.setItem(key,JSON.stringify(arr)); }catch(err){} wlForm.hidden=true; wlSuccess.hidden=false; }); }\n  // ---- accessible dropdowns: click + keyboard, with hover kept in sync via .open ----\n  document.querySelectorAll('.menu-item').forEach(function(it){ var t=it.querySelector('.trig'); if(!t)return; t.addEventListener('click',function(e){ e.preventDefault(); var open=!it.classList.contains('open'); closeMenus(it); it.classList.toggle('open',open); t.setAttribute('aria-expanded',open?'true':'false'); }); });\n  document.querySelectorAll('.dd-link').forEach(function(a){ a.addEventListener('click',function(){ closeMenus(null); }); });\n  document.addEventListener('click',function(e){ if(!e.target.closest('.menu-item'))closeMenus(null); });\n  // desktop hover drives the SAME .open + aria state, so screen readers never desync\n  if(window.matchMedia&&window.matchMedia('(hover:hover) and (min-width:901px)').matches){\n    document.querySelectorAll('.menu-item').forEach(function(it){ var t=it.querySelector('.trig');\n      it.addEventListener('mouseenter',function(){ closeMenus(it); it.classList.add('open'); if(t)t.setAttribute('aria-expanded','true'); });\n      it.addEventListener('mouseleave',function(){ it.classList.remove('open'); if(t)t.setAttribute('aria-expanded','false'); });\n    });\n  }\n\n  // Escape closes modal, open menus, and mobile nav\n  document.addEventListener('keydown',function(e){ if(e.key==='Escape'){ closeWL(); closeMenus(null); closeMobile(); } });\n\n  // hamburger toggle\n  var hbtn=document.getElementById('hbtn'),mmenu=document.getElementById('mobileMenu');\n  if(hbtn&&mmenu){ hbtn.addEventListener('click',function(){ var open=!mmenu.classList.contains('open'); mmenu.classList.toggle('open',open); hbtn.classList.toggle('open',open); hbtn.setAttribute('aria-expanded',open?'true':'false'); }); }\n  document.querySelectorAll('.mobile-menu a:not([data-plan])').forEach(function(a){ a.addEventListener('click',closeMobile); });\n})();\n\n  // ===== Interactive behaviours (vanilla, no external deps) =====\n  (function(){\n    var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion:reduce)').matches;\n\n    // ---------- Galaxy heading: one-shot slide-in (PowerPoint style) ----------\n    var gh = document.getElementById('galaxyHead');\n    if(gh){\n      var words = gh.textContent.split(' ');\n      gh.textContent='';\n      var idx=0;\n      words.forEach(function(w,wi){\n        var ws=document.createElement('span'); ws.className='gx-word';\n        for(var i=0;i<w.length;i++){\n          var c=document.createElement('span'); c.className='gx-char'; c.textContent=w[i];\n          var ang=Math.random()*Math.PI*2, dist=60+Math.random()*120;\n          c.style.setProperty('--tx',(Math.cos(ang)*dist).toFixed(1)+'px');\n          c.style.setProperty('--ty',(Math.sin(ang)*dist-30).toFixed(1)+'px');\n          c.style.setProperty('--tr',((Math.random()*2-1)*55).toFixed(1)+'deg');\n          c.style.setProperty('--d',(idx*0.018).toFixed(3)+'s');\n          ws.appendChild(c); idx++;\n        }\n        gh.appendChild(ws);\n        if(wi<words.length-1) gh.appendChild(document.createTextNode(' '));\n      });\n      if(reduce){ gh.classList.add('go'); }\n      else {\n        var gio=new IntersectionObserver(function(es){ es.forEach(function(e){\n          if(e.isIntersecting) gh.classList.add('go');\n          else if(e.boundingClientRect.top>0) gh.classList.remove('go'); // reset only when scrolled back above, so it replays next entry\n        }); },{threshold:.55});\n        gio.observe(gh);\n      }\n    }\n\n    // ---------- Primitives: spinnable dial (auto-rotates, wraps, click-to-front) ----------\n    var dial=document.getElementById('primDial');\n    if(dial){\n      var items=Array.prototype.slice.call(dial.querySelectorAll('.witem'));\n      var panes=Array.prototype.slice.call(document.querySelectorAll('.dc-pane'));\n      var N=items.length, sep=(Math.PI*2)/N;        // items evenly around full circle -> wraps\n      var rot=0, target=0, vel=0, drag=false, ly=0, lt=0, raf=null, active=-1;\n      var auto=null, hover=false, inDial=false;\n      function norm(a){ a=a%(Math.PI*2); if(a>Math.PI)a-=Math.PI*2; if(a<-Math.PI)a+=Math.PI*2; return a; }\n      function place(){\n        var box=dial.getBoundingClientRect();\n        var cx=10, cy=box.height/2, R=Math.min(box.height*0.40,230);\n        var best=1e9, bi=0;\n        items.forEach(function(it,i){\n          var a=i*sep+rot;                          // 0 => front (pointing right toward content)\n          var x=cx+Math.cos(a)*R, y=cy+Math.sin(a)*R;\n          it.style.left=x.toFixed(1)+'px'; it.style.top=y.toFixed(1)+'px';\n          var depth=(Math.cos(a)+1)/2;              // 1 at front, 0 at back\n          it.style.opacity=(0.12+depth*0.88).toFixed(3);\n          it.style.filter=depth<0.55?'blur('+((0.55-depth)*7).toFixed(1)+'px)':'none';\n          it.style.zIndex=String(Math.round(depth*100));\n          it.style.pointerEvents=depth<0.2?'none':'auto';\n          var d=Math.abs(norm(a));\n          if(d<best){best=d;bi=i;}\n        });\n        if(bi!==active){ active=bi;\n          items.forEach(function(it,i){ it.classList.toggle('active',i===bi); it.setAttribute('aria-selected',i===bi);});\n          panes.forEach(function(p){ p.classList.toggle('active',+p.getAttribute('data-i')===bi); });\n        }\n      }\n      function tick(){\n        if(!drag){ rot+=(target-rot)*0.14+vel; vel*=0.88;\n          if(Math.abs(target-rot)<0.0006 && Math.abs(vel)<0.0003){ rot=target; place(); raf=null; return; } }\n        place(); raf=requestAnimationFrame(tick);\n      }\n      function kick(){ if(!raf) raf=requestAnimationFrame(tick); }\n      function next(){ target-=sep; vel=0; kick(); }   // down / right -> next (wraps)\n      function prev(){ target+=sep; vel=0; kick(); }   // up / left -> previous (wraps)\n      function goTo(i){ target+=norm((-i*sep)-target); vel=0; kick(); } // shortest spin to front\n      function stopAuto(){ if(auto){clearInterval(auto);auto=null;} }\n      function startAuto(){ if(reduce)return; stopAuto(); auto=setInterval(function(){ if(inDial && !drag && !hover) next(); },2600); }\n      function bump(){ startAuto(); }                  // restart the auto timer after manual input\n      items.forEach(function(it,i){ it.addEventListener('click',function(){ goTo(i); bump(); }); });\n      dial.addEventListener('pointerdown',function(e){ drag=true; ly=e.clientY; lt=performance.now(); vel=0; stopAuto(); dial.setPointerCapture&&dial.setPointerCapture(e.pointerId); kick(); });\n      window.addEventListener('pointermove',function(e){ if(!drag)return; var now=performance.now(); var dt=Math.max(12,now-lt); var dy=e.clientY-ly; var dr=dy*0.006; rot+=dr; vel=dr*(16/dt); ly=e.clientY; lt=now; place(); });\n      window.addEventListener('pointerup',function(){ if(!drag)return; drag=false; target=Math.round(rot/sep)*sep; kick(); bump(); });\n      dial.addEventListener('pointerenter',function(){hover=true;});\n      dial.addEventListener('pointerleave',function(){hover=false;});\n      var dio=new IntersectionObserver(function(es){es.forEach(function(e){inDial=e.isIntersecting;});},{threshold:.35});\n      dio.observe(dial);\n      window.addEventListener('keydown',function(e){ if(!inDial)return; if(e.key==='ArrowDown'||e.key==='ArrowRight'){e.preventDefault();next();bump();} else if(e.key==='ArrowUp'||e.key==='ArrowLeft'){e.preventDefault();prev();bump();} });\n      place(); startAuto();\n      window.addEventListener('resize',place);\n    }\n\n    // ---------- Interactive canvas globe + orbiting labels ----------\n    var cv=document.getElementById('globeCanvas');\n    if(cv && cv.getContext){\n      var ctx=cv.getContext('2d'), W=cv.width, H=cv.height, R=Math.min(W,H)*0.42, CX=W/2, CY=H/2;\n      var pts=[], NP=1100, inc=Math.PI*(3-Math.sqrt(5));\n      for(var i=0;i<NP;i++){ var y=1-(i/(NP-1))*2; var rr=Math.sqrt(1-y*y); var phi=i*inc; pts.push([Math.cos(phi)*rr,y,Math.sin(phi)*rr]); }\n      var yaw=0, pitch=-0.28, base=0.0026, vYaw=base, vPitch=0, gdrag=false, lx=0, gy=0, glt=0;\n      function gdraw(){\n        ctx.clearRect(0,0,W,H);\n        if(!gdrag){ yaw+=vYaw; pitch+=vPitch; vYaw+=(base-vYaw)*0.05; vPitch*=0.92; }\n        pitch=Math.max(-1.1,Math.min(1.1,pitch));\n        var cyaw=Math.cos(yaw),syaw=Math.sin(yaw),cp=Math.cos(pitch),sp=Math.sin(pitch);\n        for(var i=0;i<pts.length;i++){\n          var x=pts[i][0],y=pts[i][1],z=pts[i][2];\n          var x1=x*cyaw-z*syaw, z1=x*syaw+z*cyaw;\n          var y1=y*cp-z1*sp, z2=y*sp+z1*cp;\n          if(z2<-0.1) continue;\n          var sx=CX+x1*R, sy=CY+y1*R, t=(z2+1)/2;\n          ctx.beginPath(); ctx.fillStyle='rgba(47,230,198,'+(0.18+0.62*t).toFixed(3)+')'; ctx.arc(sx,sy,0.5+1.7*t,0,6.2832); ctx.fill();\n        }\n        requestAnimationFrame(gdraw);\n      }\n      requestAnimationFrame(gdraw);\n      cv.addEventListener('pointerdown',function(e){ gdrag=true; lx=e.clientX; gy=e.clientY; glt=performance.now(); cv.setPointerCapture&&cv.setPointerCapture(e.pointerId); });\n      cv.addEventListener('pointermove',function(e){ if(!gdrag)return; var now=performance.now(); var dt=Math.max(12,now-glt); var dx=e.clientX-lx, dy=e.clientY-gy; yaw+=dx*0.006; pitch+=dy*0.006; vYaw=dx*0.006*(16/dt); vPitch=dy*0.006*(16/dt); lx=e.clientX; gy=e.clientY; glt=now; });\n      window.addEventListener('pointerup',function(){ gdrag=false; });\n\n      // orbiting labels: a rotating pool that fades out & reappears elsewhere\n      var gw=cv.parentElement;\n      var pool=[{t:'action.committed',c:'#2fe6c6'},{t:'awaiting approval',c:'#9a8bff'},{t:'replay \u00b7 exactly-once',c:'#2fe6c6'},{t:'crash \u2192 resumed',c:'#67d3ff'},{t:'0 duplicates',c:'#46e08f'},{t:'idempotent write',c:'#ffce6a'},{t:'compensation ran',c:'#ff85c0'},{t:'\u2713 approved by blue',c:'#9a8bff'},{t:'side effect fired once',c:'#2fe6c6'}];\n      var SLOTS=3, slots=[], pi=0;\n      function nextItem(){ var it=pool[pi%pool.length]; pi++; return it; }\n      for(var s=0;s<SLOTS;s++){\n        var el=document.createElement('span'); el.className='glabel';\n        var dot=document.createElement('span'); dot.className='mdot';\n        var tx=document.createElement('span'); tx.className='gtxt';\n        el.appendChild(dot); el.appendChild(tx); gw.appendChild(el);\n        slots.push({el:el,dot:dot,tx:tx,base:Math.random()*Math.PI*2,t0:performance.now()-Math.random()*3000,dur:4200+Math.random()*1800,item:null});\n      }\n      function cycle(sl,now){ sl.item=nextItem(); sl.tx.textContent=sl.item.t; sl.dot.style.background=sl.item.c; sl.base=Math.random()*Math.PI*2; sl.t0=now; sl.dur=4200+Math.random()*1800; }\n      slots.forEach(function(sl){cycle(sl,performance.now());});\n      function orbit(){\n        var now=performance.now();\n        var w=cv.offsetWidth||380, h=cv.offsetHeight||w;\n        var ccx=cv.offsetLeft+w/2, ccy=cv.offsetTop+h/2;\n        var rx=w*0.62, ry=h*0.42;\n        slots.forEach(function(sl){\n          var e=(now-sl.t0)/sl.dur;\n          if(e>=1){ cycle(sl,now); e=0; }\n          var ang=sl.base+(now-sl.t0)*0.00075;\n          var x=ccx+Math.cos(ang)*rx, y=ccy+Math.sin(ang)*ry;\n          sl.el.style.left=x.toFixed(1)+'px';\n          sl.el.style.top=y.toFixed(1)+'px';\n          sl.el.style.opacity=Math.max(0,Math.sin(Math.PI*e)).toFixed(3);\n        });\n        requestAnimationFrame(orbit);\n      }\n      if(!reduce) requestAnimationFrame(orbit);\n      else slots.forEach(function(sl,i){ sl.el.style.opacity=1; sl.el.style.left=(cv.offsetLeft+cv.offsetWidth*(i==0?0.1:i==1?0.8:0.45))+'px'; sl.el.style.top=(cv.offsetTop+cv.offsetHeight*(i==0?0.2:i==1?0.55:0.9))+'px'; });\n    }\n\n    // ---------- Code: continuous line-by-line typing ----------\n    var cpanes=Array.prototype.slice.call(document.querySelectorAll('.cpane'));\n    cpanes.forEach(function(p){\n      if(p.dataset.lined) return; p.dataset.lined='1';\n      var lines=p.innerHTML.split('\\n');\n      p.innerHTML=lines.map(function(l){ return \"<span class='cline'>\"+(l===''?'&nbsp;':l)+\"</span>\"; }).join('');\n    });\n    function typeLoop(p){\n      if(reduce){ p.querySelectorAll('.cline').forEach(function(l){l.classList.add('show');}); return; }\n      var lines=p.querySelectorAll('.cline'), nL=lines.length, tok={}; p._tok=tok;\n      lines.forEach(function(l){ l.classList.remove('show','caret'); });\n      var i=0;\n      (function step(){\n        if(p._tok!==tok) return;\n        if(i>=nL){ if(lines[nL-1])lines[nL-1].classList.remove('caret'); setTimeout(function(){ if(p._tok===tok) typeLoop(p); },2600); return; }\n        if(i>0) lines[i-1].classList.remove('caret');\n        lines[i].classList.add('show','caret'); i++;\n        setTimeout(step,130+Math.random()*110);\n      })();\n    }\n    function activePane(){ return cpanes.filter(function(p){ return !p.hidden; })[0]; }\n    var codeSec=document.getElementById('code'), started=false;\n    if(codeSec){\n      var cio=new IntersectionObserver(function(es){ es.forEach(function(e){ if(e.isIntersecting && !started){ started=true; var ap=activePane(); if(ap) typeLoop(ap); } }); },{threshold:.2});\n      cio.observe(codeSec);\n    }\n    document.querySelectorAll('.ctab').forEach(function(tb){ tb.addEventListener('click',function(){ setTimeout(function(){ var ap=activePane(); if(ap) typeLoop(ap); },20); }); });\n  })();\n";
