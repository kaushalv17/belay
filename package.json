{
  "name": "belay-monorepo",
  "private": true,
  "version": "0.4.0",
  "description": "Belay monorepo — reliability for AI agent actions.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "demo": "tsx examples/refund-demo.ts",
    "demo:approval": "tsx examples/approval-demo.ts",
    "demo:saga": "tsx examples/saga-demo.ts",
    "demo:workflow": "tsx examples/workflow-demo.ts",
    "migrate": "node --env-file=.env scripts/migrate.mjs",
    "migrate:status": "node --env-file=.env scripts/migrate.mjs --status",
    "db:smoke": "node --env-file=.env node_modules/tsx/dist/cli.mjs examples/db-smoke.ts"
  },
  "packageManager": "pnpm@9.12.0",
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.19.0"
  },
  "dependencies": {
    "pg": "^8.12.0"
  }
}
